polymarket-client
aiogram==3.13.1
websockets>=12.0
aiohttp==3.10.5
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0
redis==5.0.8
pydantic-settings==2.5.2
loguru==0.7.2
pytest==8.3.3
pytest-asyncio==0.24.0

# Шифрование приватных ключей пользователей (core/crypto.py).
# Без него бот не стартует: мастер /setup хранит ключи только
# в зашифрованном виде.
cryptography>=42.0.0

# Быстрый цикл событий на libuv. Бот целиком построен на ожидании
# сети, где uvloop даёт заметный выигрыш. Не работает на Windows —
# код это учитывает и молча откатывается на стандартный asyncio.
uvloop>=0.19.0
# Асинхронный DNS-резолвер для aiohttp (без него резолв идёт
# в пуле потоков).
aiodns>=3.1.0

# КРИТИЧНО для скорости подписи ордеров.
#
# Ордера Polymarket подписываются EIP-712 (ECDSA secp256k1). Библиотека
# eth_keys выбирает бэкенд автоматически: если coincurve установлен —
# считает на C, если нет — откатывается на NativeECCBackend, то есть на
# ЧИСТЫЙ PYTHON. Разница на порядки, и это происходит синхронно прямо
# в цикле событий, блокируя его на время подписи каждого ордера.
coincurve>=19.0.0